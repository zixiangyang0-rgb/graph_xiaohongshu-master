# =============================================================================
# Python 项目依赖清单
# =============================================================================
#
# 通俗理解：这是项目的"购物清单"，列出了项目需要的所有 Python 第三方包。
#
# 安装方法：
#   python -m venv venv       # 创建虚拟环境
#   source venv/bin/activate  # 激活环境（Windows 用 venv\Scripts\activate）
#   pip install -r requirements.txt
#
# 各依赖分组说明：
#   - 核心框架：FastAPI（Web 框架）、uvicorn（服务器）
#   - 异步支持：psycopg（PostgreSQL 驱动）
#   - 认证：python-jose（JWT 令牌）、passlib（密码哈希）
#   - AI：langchain、langgraph
#   - 日志：structlog
#   - HTTP：httpx（用于调用外部 API）
#   - 配置：pydantic

# -----------------------------------------------------------------------------
# Web 框架 & 服务器
# -----------------------------------------------------------------------------

fastapi>=0.109.0       # 现代 Python Web 框架，支持类型提示和自动文档生成
uvicorn[standard]>=0.27.0  # ASGI 异步服务器，运行 FastAPI 应用
python-multipart>=0.0.6     # 支持文件上传

# -----------------------------------------------------------------------------
# 数据库 & ORM
# -----------------------------------------------------------------------------

sqlalchemy>=2.0.25          # Python ORM，支持异步操作
psycopg[binary,pool]>=3.1.18  # PostgreSQL 异步驱动
aiofiles>=23.2.1           # 异步文件操作（保存生成的图片）
asyncpg>=0.29.0            # PostgreSQL 异步驱动（SQLAlchemy 用）
langgraph-checkpoint-postgres>=2.0.0  # LangGraph 状态持久化

# -----------------------------------------------------------------------------
# 配置 & 数据验证
# -----------------------------------------------------------------------------

pydantic>=2.5.0            # 数据验证（定义 API 模型）
pydantic-settings>=2.1.0   # 读取 .env 环境变量

# -----------------------------------------------------------------------------
# 认证 & 安全
# -----------------------------------------------------------------------------

python-jose[cryptography]>=3.3.0  # JWT 令牌生成和验证
passlib[argon2]>=1.7.4      # 密码哈希
bcrypt>=4.1.0               # 密码哈希后端

# -----------------------------------------------------------------------------
# AI / LangGraph
# -----------------------------------------------------------------------------

langchain>=0.1.0            # LangChain 核心
langchain-core>=0.1.10      # LangChain 核心抽象
langgraph>=0.2.0            # 构建 AI 工作流（节点+边+子图）
langgraph-checkpoint>=2.0.0  # LangGraph 状态持久化
openai>=1.12.0              # OpenAI 兼容接口
httpx>=0.26.0               # 异步 HTTP 客户端
tiktoken>=0.5.0             # Token 计数
langsmith>=0.1.0            # AI 可观测性平台
langchain-openai>=0.1.0     # LangChain OpenAI 封装（火山引擎 Doubao 用这个）
greenlet>=3.0.0             # 协程库（SQLAlchemy 异步需要）
volcengine-python-sdk>=1.0.0 # 火山引擎 SDK（图片生成用）

# -----------------------------------------------------------------------------
# 日志 & 监控
# -----------------------------------------------------------------------------

structlog>=24.1.0           # 结构化日志（输出 JSON 格式）

# -----------------------------------------------------------------------------
# 其他工具
# -----------------------------------------------------------------------------

python-dotenv>=1.0.0        # 从 .env 文件读取环境变量
